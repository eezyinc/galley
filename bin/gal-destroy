#!/bin/sh
#

path="${GALLEY_PATH:-$HOME/.config/galley}"

[ -f $path/etc/$1 ] || \
    { echo "missing $path/etc/$1 --maybe copy 'example'?"; exit 1; }
. $path/etc/$1

doctl compute droplet delete $host
