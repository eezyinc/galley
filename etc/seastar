# example config for Seastar
#

. $path/etc/defaults

host=seabuild
size=c-4
sleep=45
pkg='autogen cmake g++ guile-2.2 libidn11-dev libnuma-dev libtool libunbound-dev m4 ninja-build pkg-config stow trousers xutils-dev'

# Currently a broken URL for zlib requires using
branch=seamake
repo=https://github.com/fuzz/seastar.git
# branch=master
# repo=https://github.com/scylladb/seastar.git

# Spaces and other special characters *must* be escaped and
# double quoted for cmd--see 'etc/example'
cmd=./cooking.sh
